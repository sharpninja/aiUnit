# aiUnit REPL TUI Screenshots

These PNGs are generated from the deterministic fixture workspace created by `scripts/Capture-AiUnitReplScreenshots.ps1`.
The `wireframes/` subfolder contains PNG baselines rendered from the canonical SVG files in `docs/wireframes/aiunit-repl/` for aiUnit image comparisons.

Regenerate with:

```powershell
pwsh -NoLogo -NoProfile -File .\scripts\Capture-AiUnitReplScreenshots.ps1
```
